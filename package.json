{
  "name": "formulario-whatsapp-github",
  "version": "1.0.0",
  "description": "Formulário web que envia dados para WhatsApp via GitHub",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build completed'",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
